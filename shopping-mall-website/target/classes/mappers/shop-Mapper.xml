<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shopMapper">

	<!-- 상품 목록 -->
	<select id="goodsViewList"  resultType="com.spring.myapp.domain.GoodsVO">
<![CDATA[
	SELECT goodsCode, 
	 	goodsName, 
	 	secondClassification, 
	 	brand, 
	 	goodsPrice,
     	goodsStock,
     	goodsDescription,
     	goodsImage,
     	goodsThumbnailImage,
     	goodsAddedTime
	FROM goods
        WHERE firstClassification = #{firstClassification}
]]>
	</select>

	<select id="goodsViewOne" resultType="com.spring.myapp.domain.GoodsVO">
<![CDATA[
	SELECT goodsCode,
	 	firstClassification, 
	 	secondClassification, 
	 	brand, 
	 	goodsPrice,
     	goodsStock,
     	goodsDescription,
     	goodsImage,
     	goodsThumbnailImage,
     	goodsAddedTime
	FROM goods
	WHERE goodsName = #{goodsName}
]]>
	</select>


</mapper>
