<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberMapper">

	<insert id="signup">
		INSERT INTO member( email
		, password
		, username
		, phone
		, zipcode
		, address1
		, address2)
		VALUES( #{email}
		, #{password}
		, #{username}
		, #{phone}
		, #{zipcode}
		, #{address1}
		, #{address2})

	</insert>

<!-- 로그인 -->
<select id="signin" resultType="com.spring.myapp.domain.MemberVO">
	select
	    email, password, username, phone, zipcode, address1, address2, regedit, verify
	from
	    member
	where email = #{email}
</select>
</mapper>